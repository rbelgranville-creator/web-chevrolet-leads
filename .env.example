# Local: archivo SQLite vía @libsql/client (default ./data/app.db).
# Producción (Vercel): usá Turso — si TURSO_DATABASE_URL está definida, tiene prioridad.

DATABASE_PATH=./data/app.db
# TURSO_DATABASE_URL=libsql://tu-db-org.turso.io
# TURSO_AUTH_TOKEN=

AUTH_SECRET=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=Admin
